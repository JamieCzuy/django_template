# Setup the Initial Folder Structure

From in the repo's root folder:
```
mkdir docs
mkdir tmp
```

Create `.gitignore` file containing:
```
# Folders to ignore:
tmp/

# Files to ignore:
.DS_Store
```
